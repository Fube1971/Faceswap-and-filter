services:
  - type: web
    name: faceswapgame
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: REPLICATE_API_TOKEN
        value: TU_API_KEY_PRIVADA_DE_REPLICATE

services:
  - type: web
    name: face-swap-server
    runtime: python
    plan: free  # Puedes ajustar el plan si lo deseas
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.14
